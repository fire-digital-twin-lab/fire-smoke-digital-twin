# Data directory

Nội dung thật bị gitignore.

- `raw_ifc/`: file IFC local.
- `cfast_raw/`, `fds_raw/`: input/output simulator.
- `processed/`: artifact chuẩn hóa.
- `datasets/`: parquet, split, PyG cache.

Mỗi dataset release nên có manifest/hash và lưu ở Drive/DVC/Zenodo tùy giai đoạn.
