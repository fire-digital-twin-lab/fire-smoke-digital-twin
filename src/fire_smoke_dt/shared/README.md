# shared

Chỉ chứa contract dùng chung: enum/schema, label logic, validation, I/O, path và logging. Không chứa logic CFAST, FDS, IFC hoặc model. Mọi thay đổi public schema cần reviewer từ ít nhất hai workstream.
